[pytest]
testpaths = tests
python_files = test_*.py test_*_pytest.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers --ignore=tests/legacy
markers =
    smoke: basic import tests (runs in CI)
    integration: requires database
    slow: takes more than 2 seconds
