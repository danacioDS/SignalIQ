{
  "name": "signaliq",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
